name: la_bodeguita
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # 🔹 Firebase (versiones compatibles entre sí)
  firebase_core: ^2.27.0
  cloud_firestore: ^4.15.8

  # 🔹 Animaciones y utilidades de UI
  animate_do: ^3.1.2        # Para animaciones suaves
  badges: ^3.1.2            # Para mostrar el número del carrito
  pull_to_refresh: ^2.0.0   # (opcional, si usas 'jalar para refrescar')
  firebase_auth: ^4.16.0

  # 🔹 Mapas y ubicación
  google_maps_flutter: ^2.13.1
  geolocator: ^14.0.2
  http: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/bodeguita_icon.png
    - assets/persona_icon.png
